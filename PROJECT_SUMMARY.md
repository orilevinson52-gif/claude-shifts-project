# מערכת שיבוץ משמרות אבטחה — סיכום הפרויקט

## מה זה

מערכת לניהול ושיבוץ אוטומטי של משמרות אבטחה, שנבנתה בעקבות בקשה מקורית (ב-[gemini-code-1787660263537.txt](gemini-code-1787660263537.txt)) ליצור "Automated Resource Allocation & Load Balancing System". המערכת מנהלת אנשי צוות, תפקידים, עמדות ומשמרות, ומשבצת אוטומטית אנשים למשמרות תוך אכיפת אילוצי מנוחה, זמינות אישית ומגבלות עומס — עם ממשק בעברית מלא (RTL).

## טכנולוגיות

- **מסד נתונים:** MySQL, רץ מקומית כמופע standalone עצמאי על פורט 3307 (לא נוגע בשירות MySQL80 המערכתי)
- **Backend:** Python 3.14
- **Frontend:** NiceGUI (במקום Streamlit שנוסה בהתחלה ונמצא לא מספק ויזואלית)
- **גרפים:** Plotly
- **בדיקות:** pytest

## מבנה הפרויקט

```
├── main.py                      # נקודת כניסה, ניווט בין טאבים, עיצוב גלובלי (CSS/פונטים/צבעים)
├── config.py                    # פרטי חיבור למסד הנתונים
├── requirements.txt
├── db/
│   ├── schema.sql                # סכמת מסד הנתונים המלאה (להתקנה חדשה)
│   ├── seed_data.sql             # נתוני דוגמה
│   ├── migrate_dynamic_roles.sql # מיגרציה: הפיכת תפקידים לדינמיים
│   ├── migrate_constraints.sql   # מיגרציה: אילוצים אישיים + מגבלת שבועית לתפקיד
│   ├── migrate_drop_status.sql   # מיגרציה: הסרת שדה הסטטוס
│   └── connection.py             # שכבת גישה למסד הנתונים (כל פונקציות ה-CRUD)
├── scheduler/
│   └── allocator.py              # אלגוריתם השיבוץ (הלוגיקה העסקית)
├── pages/
│   ├── personnel_page.py         # טאב "אנשי צוות"
│   ├── roles_page.py             # טאב "תפקידים"
│   ├── positions_page.py         # טאב "עמדות"
│   ├── shifts_page.py            # טאב "משמרות" — בניית שבוע לפי ימים
│   ├── constraints_page.py       # טאב "אילוצים" — אי-זמינות אישית
│   └── schedule_page.py          # טאב "שיבוץ" — הפקת הסידור הסופי
├── tests/
│   └── test_allocator.py         # 12 בדיקות יחידה לאלגוריתם השיבוץ
└── scripts/
    ├── start_db.ps1               # מפעיל את מסד הנתונים
    └── start_app.ps1              # מפעיל את האפליקציה
```

## מסד הנתונים

חמש טבלאות:

| טבלה | תיאור |
|---|---|
| `Roles` | תפקידים דינמיים (למשל "לוחם", "מאבטח", "סמבצית"), עם `Max_Shifts_Per_Week` אופציונלי |
| `Personnel` | אנשי צוות — שם, תפקיד, סך שעות שנצברו |
| `Positions` | עמדות — שם ותפקיד נדרש |
| `Shifts_Roster` | המשמרות עצמן — תאריך, שעות התחלה/סיום, עמדה, מי משובץ |
| `Personnel_Unavailability` | אילוצים אישיים — טווח תאריכים שבו איש צוות מסוים אינו זמין |

כל הקשרים אוכפים שלמות רפרנציאלית (FK), כולל `ON UPDATE CASCADE` לשמות שמשתנים ו-`ON DELETE` מתאים לכל מקרה.

## אלגוריתם השיבוץ (`scheduler/allocator.py`)

לכל משמרת פתוחה, האלגוריתם בוחר מועמד תוך אכיפת כל האילוצים הבאים כאילוצים קשיחים:

1. **התאמת תפקיד** — רק מי שהתפקיד שלו תואם לתפקיד הנדרש של העמדה
2. **מנוחה של 8 שעות לפחות** — בין סוף המשמרת הקודמת לתחילת החדשה
3. **אי-זמינות אישית** — לא משבצים מישהו בתאריך שיש לו בו אילוץ פעיל
4. **מגבלת משמרות שבועית לפי תפקיד** — אם הוגדר מקסימום לתפקיד, לא חורגים ממנו (אופציונלי, per-role)
5. **איזון עומסים** — מבין כל המועמדים התקינים, נבחר מי שיש לו הכי מעט שעות מצטברות

משמרת שאין לה אף מועמד תקין מסומנת כ"לא ניתן לשבץ" במקום להיכשל בשקט. מכוסה ב-12 בדיקות pytest.

## מה יש בכל טאב

- **אנשי צוות** — טבלה + הוספה/עריכה/מחיקה (שם, תפקיד, סך שעות מוצג)
- **תפקידים** — ניהול חופשי של תפקידים + מגבלת משמרות שבועית אופציונלית לכל תפקיד
- **עמדות** — ניהול עמדות ושיוך תפקיד נדרש
- **משמרות** — בניית משמרות לשבוע שלם: בוחרים תאריך בתוך השבוע, ולכל עמדה בכל יום מגדירים טווחי שעות (עם גלגלת שעות עגולות בלבד), כולל כפתור "העתק לכל השבוע" לשכפול מהיר של יום אחד לכל השבוע. למטה — רשימת כל המשמרות עם עריכה/מחיקה בודדת
- **אילוצים** — הוספת תאריכים שבהם איש צוות מסוים אינו זמין (חופשה, מבחן וכו')
- **שיבוץ** — כפתור "צור סידור עבודה" שמריץ את האלגוריתם; התוצאה מוצגת כטבלה נפרדת לכל עמדה, כאשר העמודות הן ימי השבוע והשורות הן טווחי השעות (כל תא מציג את השם המשובץ); בתחתית — גרף עמודות של איזון השעות בין כל אנשי הצוות

כל טאב כולל גם כפתור "נקה/מחק הכל" עם אישור, למחיקה מרוכזת.

## עיצוב

עיצוב מותאם אישית בהשראת "עמדת בקרה" מבצעית — לא ה-Quasar הגנרי של NiceGUI. נבנה מוקאפ עיצובי דרך Claude Design (כ-Artifact נפרד) ולאחר מכן יושם בפועל בקוד:

- פלטת צבעים בהירה עם אמבר לפעולות עיקריות, טורקיז לנתונים/גרפים, אדום לתאים שדורשים תשומת לב
- שלושה פונטים: Rubik לכותרות, Heebo לטקסט/עברית, IBM Plex Mono לשעות ותאריכים (עם `unicode-bidi: isolate` כדי שיקראו נכון משמאל-לימין בתוך עמוד RTL)
- RTL מלא בכל הרכיבים, כולל טאבים, טבלאות וטפסים

## הרצה

```powershell
.\scripts\start_db.ps1     # מפעיל את מסד הנתונים (פורט 3307)
.\scripts\start_app.ps1    # מפעיל את האפליקציה (http://localhost:8080)
```

הרצת בדיקות:

```powershell
.\venv\Scripts\python.exe -m pytest tests\test_allocator.py -v
```

## החלטות מרכזיות לאורך הדרך

- **Streamlit → NiceGUI**: המעבר נעשה כי Streamlit נראה גנרי מדי ולא איפשר עיצוב מותאם
- **MySQL עצמאי על פורט 3307**: כי לא היו הרשאות root לשירות ה-MySQL80 הקיים במחשב, והוקם מופע נפרד שלא פוגע בו
- **תפקידים הפכו לדינמיים** (ולא ENUM קבוע) כדי לאפשר כל תפקיד חופשי, כולל מגבלת משמרות שבועית אופציונלית
- **שדה "סטטוס" הוסר** מאנשי הצוות — התייתר לאחר שנוספו אילוצים אישיים מדויקים יותר לפי תאריך
- **תצוגת השיבוץ עברה כמה גרסאות** עד שהתייצבה על טבלה לפי עמדה, עם ימים כעמודות ושעות כשורות — הפורמט הכי ברור לעין למי שומר מתי ואיפה
